#include <stdio.h>

/**
 * main - main
 * Description: single digit numbers of base 10
 * Return: 0
 */
int main(void)
{
	int z;

	for (z = 48; z <= 57; z++)
	{
		putchar(z);
	
	}

	for (z = 97; z <= 102;z++)

	while (c <= '
	{
		putchar(c);
		c++;
	}

	putchar('\n');
	return (0);
}
